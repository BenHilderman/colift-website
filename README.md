# CoLift — marketing site

Static, single-page BeReal-style site (plain HTML/CSS/JS, no build step) for GitHub Pages.
See `DEPLOY.md` for the 3 placeholders to fill (Web3Forms key, App Store URL/flag, domain) and the deploy steps.
